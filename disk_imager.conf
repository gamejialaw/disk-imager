# Default source disk to image.
SOURCE_DISK=/dev/nvme0

# Root directory where backup sets will be stored.
BACKUP_ROOT=/root/samba

# Prefix used when auto-generating backup folder names.
NAME_PREFIX=disk-image
